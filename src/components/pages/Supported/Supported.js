import React, { Component } from 'react';

class Supported extends Component {
    render() {
        return (
            <div>
                SUPPORTED
            </div>
        );
    }
}

export default Supported;